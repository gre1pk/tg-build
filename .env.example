# Set to "true" for local development with mock data (no API calls).
VITE_USE_MOCK_DATA=true

# Server-only secrets (used by /api routes in dev and on Vercel — never expose to client)
TELEGRAM_BOT_TOKEN=
AUTH_JWT_SECRET=

# App version shown on error screen (optional)
VITE_REACT_APP_VERSION=
